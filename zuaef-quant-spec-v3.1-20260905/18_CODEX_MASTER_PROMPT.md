# CODEX MASTER PROMPT — ZUAEF Quant v3.1

You are extending an already-running Quant/Agent system, not rebuilding it from this spec.

Reviewed Git baseline:
GitHub main as reviewed on 2026-09-05 17:20 +08; latest push at 2026-09-05 15:43 +08: `feat(quant): add quant-to-Telegram event bridge, v3.0 spec pack, and ops wiring`.
The local running tree may be ahead; verify it first.

Before ANY edit, create `BASELINE_RUNTIME.md` containing:
- branch/HEAD and origin/main relationship;
- git status and uncommitted/unpushed work;
- monitor/serve/bridge services and timers;
- canonical state paths;
- candidate snapshot/handoff;
- six-tool QuantDecision composition;
- Workbench endpoints;
- bridge state/log paths;
- latest report and verifiable current state;
- PIT/data-trust status;
- proactive-delivery proof status;
- discrepancies vs this pack.

Do not reset/clean/stash/checkout/merge/overwrite before preserving runtime truth.

Preserve current contracts:
- M1 lifecycle/status taxonomy;
- ack-buy/ack-sell/skip;
- D+1/3/5/8 settlement;
- loopback-only Workbench writes;
- six model-visible QuantDecision tools;
- Bridge single proactive delivery authority;
- E1/E2 Agent interpretation-only behavior;
- deterministic fallback/checkpoint delivery;
- Dashboard/Telegram as projections, not truth.

Mission order:
1. preserve/prove current runtime;
2. Agent surface gap audit;
3. evidence namespaces;
4. strict PIT replay seam;
5. recent 10-day production-equivalent replay;
6. replay/live-forward isolation proof;
7. Market Regime shadow with NEW field;
8. Evidence Retrieval v1;
9. Experiment Registry around existing evaluator;
10. S2 shadow + HUMAN_SKIP analysis.

DO NOT:
- add a 7th model-visible quant tool before gap audit;
- expose Workbench writes beyond loopback;
- call current `csi500_subset` evaluation strict PIT replay;
- project current constituents backward silently;
- count replay/shadow as live-forward;
- reuse `market_no_trade` for Regime;
- invent a different settlement horizon;
- weaken production thresholds to manufacture triggers;
- mark proactive Bridge PROVEN before real-session acceptance;
- implement autonomous real-money execution.

Completion evidence:
changed files/reasons, commands/tests, regression proof, gap audit, 10-day replay,
future-leak tests, namespace isolation, six-tool compatibility, loopback security,
shadow-only Regime proof, limitations, and final state:
`PROMOTE / CONTINUE_SHADOW / REJECT / BLOCKED`.
