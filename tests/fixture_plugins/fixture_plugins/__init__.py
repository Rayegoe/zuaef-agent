"""Fixture plugins for the Plugin Composition Layer tests.

These are real importable modules whose ``zuaef.plugins`` entry points are
constructed in the test suite (hermetic — nothing is pip-installed). The
modules double as import counters for the no-implicit-activation contract.
"""
