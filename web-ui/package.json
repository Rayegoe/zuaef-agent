{
  "name": "zuaef-console",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "smoke": "npm run build && node smoke/browser-smoke.mjs"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.1.0"
  }
}
