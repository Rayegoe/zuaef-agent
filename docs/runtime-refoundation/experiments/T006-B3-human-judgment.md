# T006-B3 — Blind human quality / evidence judgment

Status: HUMAN VERDICT RECORDED — `Z > X > Y`; evidence remains unclear.

Date: 2026-08-21

## Blind ranking

```text
Z > X > Y
```

The reviewer preferred Z because it most clearly rebuilt a relationship from
the materials instead of repeatedly applying the pattern
`understand -> summarize -> framework -> conclude`.

### Z

- strongest authorial progression;
- established the tension between falling supply-side construction cost and
  unchanged enterprise delegation trust;
- used Dana / 300 emails / 10 requirement emails / 20 suppliers / four
  lookalike companies / 42 checks as an actual scene and argument, not merely
  an example;
- moved from that case into mechanisms rather than repeating a source summary;
- weakened in the final 15% when Harness Engineering became too close to a
  universal explanation;
- contained over-strong necessity language (`any layer` / `cannot`) and an
  ambiguous final use of organizational responsibility.

### X

- strongest evidence discipline and clear logic;
- correctly separated platform openness, technical ability and actual
  enterprise delegation;
- lost authorial presence through a textbook-like four-barrier structure;
- grouped economic viability with failure mechanisms mainly to make the
  structure symmetrical;
- ended in predictable governance / audit language.

### Y

- superficially smooth and complete;
- most often converted each material block immediately into a generalized
  conclusion;
- expanded one benchmark into broad claims about where model intelligence
  lives and a `cliff-like` decline;
- forced the material into three layers and a model-versus-system binary;
- made the strongest unsupported assertion that the Agent does not lack
  capability and only lacks the surrounding system.

## Evidence judgment

The reviewer had the anonymous drafts and rubric but not the complete desk
pack, so no draft received an evidence pass:

| anonymous draft | evidence verdict |
|---|---|
| X | `unclear` |
| Y | `unclear / expansion risk` |
| Z | `unclear / localized risk` |

Evidence-discipline ranking: `X > Z > Y`.

Quality ranking: `Z > X > Y`.

## Mapping revealed after judgment

```text
X = model-owned eager catalog
Y = Host-selected techniques
Z = technique OFF
```

## Interpretation boundary

This is a comparative quality win for technique OFF on this real task, not a
global OFF promotion:

- T006-B1 previously found Host-selected ON clearly better than OFF on the
  product-story fixture;
- T006-B2 found model-owned better than Host, with evidence failure and a
  `limit_reached` terminal state;
- T006-B3 finds OFF better than model-owned eager catalog, which is better than
  Host, while evidence remains unclear for all three.

The stable observation is therefore task dependence plus eager-context cost,
not a universal technique winner. In B3 the eager model-owned catalog consumed
3,187 first-request characters, was tail-truncated, and the model selected zero
technique IDs. The smallest next hypothesis is that technique knowledge should
be model-owned and observed only on demand, leaving the initial desk pack at
the OFF shape.

## Verdict

```text
REFINE
NO_PRODUCTION_PROMOTION
EVIDENCE_UNCLEAR
NEXT_EXPERIMENT: LAZY MODEL-OWNED CATALOG
T007_DEFERRED
```
