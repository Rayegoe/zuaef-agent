# Legacy benchmark profile. Production uses ``ace-writing.toml`` and never
# enables CodeMode. This profile wraps only ``pull_context``; ``save_article``
# stays explicit.
schema = 1
name = "ace-writing-codemode"

[[plugins]]
id = "ace-writing"
allow_capabilities = true

[plugins.config]
code_mode = true
